# PR #1246 跟进

目标：处理单词本 PR 的当前冲突、两条未解决评审意见及三项并发编译警告。
用户授权本地跟进；不推送、不发布评论。

初始工作树干净，HEAD 为 `37c59bb8`（`docs/wordbook-design`）。
以远端 PR `db8745ba` 创建本地合并分支，合入 `tisfeng/dev` 的 `e812288f`。
其他 worktree 保持原状。上游文件改动属于本次合并，不额外重构。

允许路径：基础查询控制器、语言选择单元格、HostWindowManager、Wordbook
星标与两个 actor 的初始化、相关概览及架构图、工程引用、本计划和同任务 history。

- [x] 核对 GitHub 评论、review thread、PR head/base 和检查状态。
- [x] 追溯冲突，保留上游反向翻译需要的结果读取方法。
- [x] 修复首次窗口激活、回放后语言重选及三个并发警告。
- [x] 同步目录文档并进行独立审查，无新增阻塞项。
- [x] 73 项测试 / 7 个套件通过；格式、工程和合并一致性检查通过。
- [x] 本地修复完成，冻结交付快照；GitHub 仍待用户授权推送。

风险：语言选择不能把未改动的另一侧恢复成全局偏好；窗口首次创建与复用
必须共享激活路径；保留旧收藏迁移契约及上游反向翻译行为。

完成标准：冲突标记清零，相关验证通过，review 无未处理的有效阻塞项，
以同任务 history 记录实际结果和验证限制。
