# 更新用户文档

- 状态：completed
- 创建日期：2026-09-13
- 负责人：Codex
- 关联 Issue/PR：none

## 背景

中英文完整使用指南长期未随产品功能同步，现有服务清单、语言数量、OCR、Apple 翻译、设置页、
快捷键和贡献链接已与当前实现产生偏差。服务说明还包含容易过期的免费额度、旧版命令配置和
第三方词典下载信息。

## 目标与范围

- 目标结果：以当前代码为准更新中英文公共文档，并减少易漂移的重复信息。
- 允许修改路径：`docs/user-docs/`、`README.md`、`README_ZH.md`、本计划及对应 history。
- 同任务 history：`docs/histories/2026-09/2026-09-13-refresh-user-docs.md`
- 用户限制：先前只读审计已获确认执行；不 push。
- 非目标：不修改产品代码、运行时资源、赞助历史或外部服务配置。
- 验收标准：中英文服务集合一致，主要功能说明与当前实现一致，内部链接和锚点有效，静态检查通过。

## 工作计划

1. 新增中英文服务总览，将服务类型、凭据和用途集中到单一详细入口。
2. 收敛中英文完整使用指南，更新语言、OCR、TTS、设置、快捷键及现有功能。
3. 重写 Apple 翻译说明，拆分 Apple Dictionary 与 Easydict MDict 的使用边界。
4. 修复本地化贡献指南链接，并同步公共文档索引和根 README 摘要。
5. 校验中英文服务覆盖、Markdown 链接与锚点、格式和 Git 范围，记录 history 并提交。

## 风险与决策

- 不复制厂商免费额度、模型列表和接口稳定性结论，避免公共文档随外部政策频繁失效。
- 服务总览按用户使用方式分类；注册表中的润色、总结等能力不冒充独立翻译厂商。
- 保留 Apple `.dictionary` 的合法导入说明，移除来源不明的第三方词典下载链接；MDX/MDD 改为直接导入 Easydict。
- 根 README 只保留类别摘要并链接服务总览，完整服务列表只在对应语言的 `SERVICES.md` 维护。

## 进度

- [x] 新增服务总览和 MDict 指南。
- [x] 更新中英文完整使用指南和专题文档。
- [x] 同步索引与根 README。
- [x] 完成静态验证、history 和本地提交。

## 验证

- 中英文 `SERVICES.md` 各 29 个服务 ID，与 `EZEnumTypes.m` 服务注册集合一致。
- 语言注册表扣除自动检测和文言文后为 52 个可选翻译语言。
- 17 份根级和 `docs/user-docs/` Markdown 的相对链接与锚点检查通过。
- 旧语言数量、旧系统要求、旧免费额度、来源不明下载链接和旧快捷键残留检索通过。
- `git diff --check`：通过。
- `xcodebuild`：未运行；本任务仅修改不进入 Xcode 构建图的公共 Markdown。

## 完成条件

- [x] 计划内文档全部更新且中英文结构一致。
- [x] 静态检查通过，没有暂存用户已有的 `AGENTS.md` 修改。
- [x] 计划归档到 `docs/exec-plans/completed/2026-09/`，对应 history 和本地提交完成。
