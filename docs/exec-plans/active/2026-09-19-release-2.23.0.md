# Easydict 2.23.0 Draft Release

<!-- 文件名：YYYY-MM-DD-<slug>.md；命名规则见 docs/histories/README.md 的“命名与 slug”。 -->

- 状态：active
- 创建日期：2026-09-19
- 负责人：Unknown
- 关联 Issue/PR：2.23.0 release

## 执行上下文

- **Agent Name:** `/root`
- **Model:** Unknown
- **Environment:** `macOS 27.0 / Xcode 27.0 (27A266a)`

## 背景

Easydict 2.22.0 之后已有一批用户可见功能和修复，当前 `dev` 没有 2.23.0 canonical changelog、Draft 或版本 Tag。需要按 release-easydict Skill 创建 beta Draft，并在发布前停止。

## 目标与范围

- 目标结果：创建并验证 2.23.0 beta GitHub Draft Release。
- 允许修改路径：`changelog/2.23.0.md`、本计划及本任务 history、Draft 工作流临时状态。
- 同任务 history：`docs/histories/2026-09/2026-09-19-release-2.23.0.md`
- 用户限制：只创建 Draft，不 publish，不处理 Issue，不 push `dev` 或 `main`。
- 非目标：不修改应用生产代码，不发布公开 Release。
- 验收标准：Draft、临时发布分支、版本 Tag、changelog 与正文哈希均通过远程/本地验证。

## 工作计划

1. 收集 2.22.0 之后已合并 PR，整理英文 changelog 并验证格式。
2. 将 changelog 和计划提交到本地 `dev`，运行 Draft 工作流。
3. 预览并应用简洁英文 Draft 标题，刷新验证 Draft 正文与引用。
4. 更新并归档计划，记录 history；不执行 publish 或 Issue follow-up。

## 风险与决策

- 默认使用 `beta` channel。
- Draft 工作流在隔离 worktree 中构建、签名、公证和打包，当前 checkout 保持不变。
- changelog 是 GitHub Release 正文和 Sparkle description 的唯一来源，冻结后不直接编辑远程正文。

## 进度

- [ ] 创建并验证 canonical changelog。
- [ ] 创建并验证 GitHub Draft。
- [ ] 更新 history 并归档本计划。

## 验证

- 运行 `python3 scripts/release/release_notes.py validate --file changelog/2.23.0.md --version 2.23.0`。
- 运行 Draft 工作流并核对输出的 run ID、Draft URL、Tag、临时分支和 notes SHA-256。

## 完成条件

- Draft 创建完成且正文与 canonical changelog 一致。
- 远程 `release/sync-2.23.0` 与 Tag 已验证。
- 未执行公开发布；失败时保留 run ID 和 `.tmp/release/2.23.0/` 恢复状态。
