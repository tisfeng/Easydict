## 2026-09-17 | 任务：整理菜单栏操作分组

**Links:** None

### 执行上下文

- **Agent Name:** `Codex`
- **Model:** `GPT-5`

### 用户请求

按用户任务重新整理菜单栏操作，并仅在鼠标划词窗口类型与快捷键划词窗口类型不同时显示
“显示迷你窗口”。

### 变更

- 将查询入口、截图与 OCR、文本处理和应用管理操作按任务分组，并保持原有动作与快捷键绑定。
- 订阅鼠标划词与快捷键划词窗口类型，仅在两者不同时显示“显示迷你窗口”。
- 未新增或修改测试代码。

### 设计意图

菜单继续保持扁平的一步操作，通过顺序和分隔线表达类别。窗口类型使用现有 Defaults 设置作为
唯一状态来源，因此菜单可见性会随设置变化更新，不引入额外通知或重复状态。

### 验证

- `xcodebuild build -workspace Easydict.xcworkspace -scheme Easydict | xcbeautify`：在沙箱外通过，
  包含 Format、Lint、资源编译、应用签名和产物验证。
- `git diff --check`：通过。
- 静态检查：两个窗口类型相同时隐藏“显示迷你窗口”，不同时显示；其他菜单动作和快捷键绑定保持不变。
- 自动化测试：未运行；现有测试没有覆盖菜单视图，本次任务未获授权新增或修改测试代码。

### 受影响文件

- `Easydict/Swift/View/MenuItemView.swift`
- `docs/histories/2026-09/2026-09-17-organize-menu-bar-actions.md`

### 后续事项

- None
