## 2026-09-10 | 任务：适配已有提交的 Git 集成流程

### 用户请求

参考 skills 项目「改进技能和子代理」任务，适配 Easydict 的宿主规则，保留现有完整输出格式。

### 变更

- Git 交付优先传递精简独立上下文，区分新提交候选与已有提交的集成范围。
- 实际加载的 Agent、Skill 和配套脚本共同支持时，允许一次委派完成已有提交的检查与集成。
- 保留新提交预览、旧版本输入契约和异常停止规则；已知受限写入直接申请必要提权。
- 完整回执一次收集，由主 Agent 批量核验后按原有模板呈现。

### 设计意图

本次只适配宿主规则，不复制上游未发布的受管文件。检查时 skills 上游已经实现
`phase=integrate` 与 `inspect-integration.py`，本仓库两个 lock 仍指向 `v0.3.2`，
因此当前继续使用旧版 `prepare/apply`；完整升级后才可使用一次委派流程。

### 验证

- 已完成独立 planner 评估，确认旧版本兼容与最小修改范围。
- `git diff --check` 与相对链接、锚点检查通过；受管 Skill、Agent 和两个 lock 均保持不变。
- 独立 reviewer 复核未发现需要修复的问题，确认旧版兼容、新版能力条件和完整回执契约。
- 本次没有运行 Git 集成、Xcode 构建或性能基准，不声称启用了新流程或达成提速目标。

### 受影响文件

- `docs/agents/git-workflow.md`
- 本 history。

### 后续事项

- 上游发布并获授权同步受管资产后，验证新运行时能发现更新的 Agent 和配套 Skill。
