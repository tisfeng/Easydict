# AGENTS.md

Easydict 是一款 macOS 词典和翻译应用，支持查词、文本翻译、划词翻译和 OCR 截图翻译。

`AGENTS.md` 是 Agent 的唯一任务入口；详细规则只在对应专题文档维护。

## 任务模式

### 计划模式

- 用户要求方案、分析、解释或评估时，只读取和检查现状，不修改文件、Git 或外部服务。
- 计划模式只输出方案，不创建 plan 或 history；仓库内计划与历史文件只在用户授权执行后，
  按 `docs/agents/README.md` 的规则判断和维护。

### 执行模式

- 用户要求修改、修复、更新、实现或执行时，完成范围内的修改和必要验证；验证通过后自动创建
  本地提交，用户明确要求不提交或没有差异时除外。
- 首次写入前，按“任务路由”读取当前任务所需的专题规则。任何可能产生仓库差异的执行任务都必须
  读取 `docs/agents/README.md` 的 Plan 与 History 规则。
- 需要 plan 时，在首次写入前创建或复用 active plan；产生仓库差异时，在任务完成前创建或更新
  对应 history。具体判定、命名和归档规则以专题文档为准。

### 模式切换

- 用户从计划模式切换到执行模式时，必须在首次写入前重新判断任务范围、风险、专题规则以及
  plan/history 要求，不能直接沿用计划模式上下文跳过执行前检查。
- 无法确定用户是否授权修改时保持计划模式，并请求用户明确执行范围。

### 通用边界

- 用户的禁止、范围和顺序要求优先。push、创建 Pull Request、发布及其他外部写入仅在用户明确
  要求时执行。
- 回复使用用户当前请求的语言；已有文档保持原语言，公共文档遵循 `en/zh` 目录。代码标识、
  API 名称、命令、路径、品牌名称和固定输出契约保留原文。

## Review

- 生产代码、复杂逻辑、跨模块或高风险变更完成必要验证后，提交前使用 `review` 技能审查。
- 发现有效 finding 时，在当前任务范围内修复并重新验证、审查；没有阻塞 finding 后再提交。
- 纯文档、注释、格式调整和简单低风险变更不要求审查；用户的明确要求优先。

## 任务路由

- 只读取当前任务需要的专题规则。
- 构建、测试、工程文件与资源、Xcode 验证：`docs/agents/build-and-test.md`。
- 跨语言代码质量、Swift、Objective-C、SwiftUI、API 和本地化：`docs/agents/coding-guidelines.md`。
- 文档分层、计划、history、参考资料、外部 Skills 和同步边界：`docs/agents/README.md`。
- 产品代码、跨功能行为或模块边界：`docs/design-docs/application-architecture.md`。
- 公共使用或贡献者文档：`docs/user-docs/en/` 或 `docs/user-docs/zh/`。
